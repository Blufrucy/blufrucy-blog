// Minimal stub — regenerated during plugin installation step of the build.
// This exists to satisfy static imports before plugins are installed.

export const CustomOgImagesEmitterName = "customOgImages"

export const plugins: Record<string, Record<string, (...args: unknown[]) => void>> = {}

export {}
